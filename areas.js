const PI = 3.1416;
let r = 5;
let areaOfCircle = PI * r * r;
console.log(areaOfCircle);

/**
 * Another Way of Doing This
 */
let anotherWayOfArea = Math.PI * Math.pow(r, 2);
console.log(anotherWayOfArea);

/**
 * Homework:
 * Calculate the area of Rectangle, Square, and Triangle.
 */
